import 'package:flutter/material.dart';

void main() => runApp(test_app());

class test_app extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return MaterialApp();
  }
}
